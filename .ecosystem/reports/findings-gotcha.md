# gotcha findings (52)

- [high] apps/saltgrass-modular/src/pages/about.astro:36 — All stats (3+ years, 50+ projects, 99% satisfaction) are hardcoded copy. If this data changes, it requires a code edit rather than a CMS update — drift risk over time.
- [high] apps/saltgrass-modular/src/pages/about.astro:171 — Team section uses placeholder gradient divs instead of actual photos for Engineering Team and Installation Specialists — placeholder state shipped to production.
- [med] apps/saltgrass-modular/src/pages/about.astro:16 — Images (/img/about-hero.jpg, /img/dylan-portrait.jpg, /img/facility-tour.jpg) are referenced but their existence in /public is not verifiable from this file alone — broken image risk if assets are not deployed.
- [high] apps/saltgrass-modular/src/pages/api/health.ts:51 — uptime field is set to startTime (Date.now() at request start) rather than process uptime or a stable timestamp — this will always reflect request-start epoch ms, not actual service uptime.
- [high] apps/saltgrass-modular/src/pages/api/health.ts:44 — statusCode logic sets 503 for both 'timeout' and 'error' states — the comment and overallStatus distinguish them but HTTP callers only see 503, losing the timeout signal.
- [high] apps/saltgrass-modular/src/pages/api/s.ts:10 — Regex rewrite `var u="[^"]+"` assumes the backend tracking script uses a specific variable name/format. If the backend script changes that variable name, the rewrite silently fails and the beacon URL is not patched — tra
- [high] apps/saltgrass-modular/src/pages/api/s.ts:18 — On backend error, returns HTTP 204 with empty body and Content-Type: application/javascript. Browsers will execute the empty response silently — no JS error but tracking is fully dead. Silent failure is intentional per c
- [med] apps/saltgrass-modular/src/pages/api/t.ts:10 — Content-Type is hardcoded as 'application/json' when forwarding to backend, but request.text() reads the raw body regardless of incoming Content-Type. If the tracking script sends non-JSON, the backend may reject it.
- [low] apps/saltgrass-modular/src/pages/api/t.ts:13 — res.body is streamed directly as the response. If the backend returns a streaming or chunked response, edge-runtime stream piping behavior in Astro SSR may vary.
- [med] apps/saltgrass-modular/src/pages/blog/[slug].astro:52 — CTA gradient detection: `ctaIsTailwindGradient` checks if gradient starts with 'from-' to distinguish Tailwind class from inline CSS. If a Tailwind gradient class doesn't start with 'from-' (e.g. 'bg-gradient-to-r from-.
- [high] apps/saltgrass-modular/src/pages/blog/[slug].astro:15 — getStaticPaths calls getBlogPosts() which uses limit=100. Sites with >100 posts will silently drop slugs from the pre-rendered set — those posts will 404 at runtime.
- [high] apps/saltgrass-modular/src/pages/blog/index.astro:5 — getBlogPosts() uses limit=100. If the location has >100 posts, the listing silently truncates — no pagination UI is rendered. The filter/search only operates on the pre-fetched 100-post window.
- [med] apps/saltgrass-modular/src/pages/blog/index.astro:10 — Featured post selection: `posts.find(p => p.heroStyle === 'featured') || posts[0]`. If no post has heroStyle='featured', the newest post (posts[0]) is featured by API return order — this is implicit and may surprise cont
- [med] apps/saltgrass-modular/src/pages/blog/index.astro:13 — Category fallback logic derives categories from post data if API returns empty array. The fallback uses post.category values directly as both slug and name, so category slugs will not be URL-safe if post.category contain
- [high] apps/saltgrass-modular/src/pages/blog/rss.xml.ts:9 — getBlogPosts() fetches up to 100 posts from the backend (limit=100) but rss.xml.ts only renders the first 30 (.slice(0, 30)). If there are more than 100 posts, newer posts beyond the 100-limit will never appear in the fe
- [high] apps/saltgrass-modular/src/pages/blog/rss.xml.ts:4 — The SITE_URL is hardcoded to 'https://www.saltgrassmodular.com' (line 4), not pulled from an env var. If the site is ever redeployed under a different domain or staging URL, all RSS item links and the self-referencing at
- [high] apps/saltgrass-modular/src/pages/contact.astro:101 — The LPAI calendar widget at line 100-113 is a placeholder stub — it renders a text message stating the calendar will be configured during account setup. No actual embed or iframe exists. Visitors who scroll to 'Schedule 
- [high] apps/saltgrass-modular/src/pages/contact.astro:182 — Both form submit handlers (QuoteRequestForm + ConsultationForm) perform the honeypot check then silently show the success state without actually submitting — a bot that fills the honeypot gets a false-positive success UX
- [med] apps/saltgrass-modular/src/pages/developers.astro:72 — The page claims '50+ completed projects' and '99% client satisfaction' (lines 72-78) as hard-coded marketing copy. These figures are not sourced from a CMS or backend and will silently become stale as the business grows 
- [high] apps/saltgrass-modular/src/pages/developers.astro:270 — The case study at lines 270-302 is a generic placeholder ('12-Unit Multi-Family Development, Oklahoma') with no real client name, photos, or links. The testimonial is attributed only to '— Residential Developer'. If Salt
- [high] apps/saltgrass-modular/src/pages/financing.astro:299 — The 'Schedule Call' CTA on line 299-303 uses an inline onclick targeting 'consultation-modal' which is only rendered in index.astro. If a user navigates directly to /financing the modal DOM element will not exist and the
- [med] apps/saltgrass-modular/src/pages/financing.astro:138 — The Hearth link opens externally with no tracking/UTM parameters, so conversions from this page cannot be attributed in analytics.
- [low] apps/saltgrass-modular/src/pages/homeowners.astro:257 — Native HTML <details>/<summary> accordion pattern used for FAQ (lines 257-315). This is a different UI pattern from the rest of the site which uses custom JS components — no visual consistency enforcement.
- [high] apps/saltgrass-modular/src/pages/index.astro:45 — Modals (quote-modal, consultation-modal) are only rendered in index.astro DOM. Other pages (financing.astro, and possibly others) trigger these modals via inline onclick or data-action, but those modal divs won't exist u
- [med] apps/saltgrass-modular/src/pages/index.astro:87 — The data-action='quote-modal' dispatch pattern in the client <script> (lines 82-101) uses document.getElementById which returns null if the element is missing — fails silently. No fallback to a full /contact page is prov
- [high] src/pages/military.astro:272 — Contact CTAs bypass the /contact form entirely — phone and email links are hardcoded to Dylan Walker's personal contact info. If the contact owner changes, this page requires a manual code edit. No LPAI backend contact c
- [high] src/pages/military.astro:10 — Keywords prop is passed to BaseLayout (line 10) but container-homes.astro and pools.astro do not pass keywords — inconsistency across sibling pages in whether the keywords SEO prop is used.
- [high] src/pages/models/container-homes.astro:6 — All model pricing and specs are hardcoded in the Astro frontmatter array (lines 6-75). There is no CMS or API backing this data — any price or spec change requires a code deploy.
- [med] src/pages/models/container-homes.astro:87 — Hero image references /img/models-hero.jpg (line 87) — static asset with no fallback; 404 if missing from public/img/.
- [high] src/pages/models/pools.astro:284 — scrollToSection() is defined in a <script> block (line 284) and called via inline onclick attributes (lines 44, 49). This is a global function attached to window — works but could collide if other scripts define the same
- [high] src/pages/models/pools.astro:6 — All pool model data (tubeSteelModels, weldedSteelModels arrays lines 6-16) is hardcoded — same no-CMS pattern as container-homes.astro. Price changes require code deploys.
- [med] src/pages/process.astro:228 — The FAQ mentions 'See our Financing page' (line 228) without a hyperlink — the text assumes a /financing route exists (which it does in this app) but the cross-link is plain text, reducing discoverability.
- [med] src/pages/projects.astro:163 — Category filter onclick (line 163) uses template literal string `filterProjects('${cat.id}')` but the handler references implicit `event.target` (line 303) — if the click registers on a child element of the button, event
- [high] src/pages/projects.astro:19 — All 6 project entries use placeholder CSS gradient colors for images (e.g. 'gradient-to-br from-blue-400 to-blue-600') instead of real photos (line 19). The image field is consumed as a Tailwind class string appended to 
- [high] apps/saltgrass-modular/src/pages/qr/[code].astro:16 — The catch block is a bare no-op; any network error (Railway down, DNS failure) silently returns 404 to the user with no logging, making it impossible to distinguish a bad QR code from an infrastructure outage at the page
- [high] apps/saltgrass-modular/src/pages/saltgrass-101/[slug].astro:21 — All guide content (titles, descriptions, section text, pricing figures) is hardcoded in the JS object on lines 21-110. Any content update requires a code change and redeploy rather than a CMS update — this will become fr
- [med] apps/saltgrass-modular/src/pages/saltgrass-101.astro:67 — The hero image `/img/saltgrass-101-hero.jpg` is referenced here and identically in the detail page. If the asset is missing at build time, both index and all 8 detail pages will render broken images silently (no fallback
- [high] apps/saltgrass-modular/src/pages/services/container-homes.astro:123 — The three 'Request Quote' buttons (lines 123, 138, 152) are plain `<button>` elements with no form, href, JS handler, or event listener attached. Clicking them does nothing. This is a dead CTA.
- [high] apps/saltgrass-modular/src/pages/services/container-homes.astro:164 — The 'Recent Projects' gallery section (lines 164-196) uses CSS gradient placeholders (`bg-gradient-to-br from-blue-400 to-blue-600 h-64`) instead of real project photos — these are unfilled placeholder blocks that may sh
- [high] apps/saltgrass-modular/src/pages/services/container-homes.astro:111 — The 'Popular Models' gallery (lines 111-159) also uses gradient color blocks (`bg-gradient-to-br`) instead of real product images — same placeholder-shipping risk.
- [high] apps/saltgrass-modular/src/pages/services/developers.astro:269 — The CtaBanner at line 269 prompts developers to 'Submit your RFQ or schedule a developer call' but this page contains no form, no RFQ submission mechanism, no mailto link, and no href to an RFQ form — the CTA has no acti
- [med] apps/saltgrass-modular/src/pages/services/developers.astro:14 — The hero image `/img/developers-hero.jpg` is referenced but not verified to exist in the public assets — same silent-broken-image risk as other service pages.
- [med] apps/saltgrass-modular/src/pages/services/developers.astro:150 — The developer-facing copy references `/financing` and implies government/GSA schedule availability, but no links to those pages appear in the body — a developer landing here has no in-page navigation to supporting docume
- [med] apps/saltgrass-modular/src/pages/services/disaster-relief.astro:15 — The hero image `/img/disaster-relief-hero.jpg` is referenced but existence of the asset is unverified — same silent-broken-image risk as other pages using placeholder or missing images.
- [high] apps/saltgrass-modular/src/pages/services/pools.astro:58 — 'Request Quote' buttons (lines 58, 96) are plain <button> elements with no href, form binding, or onclick handler — clicking them does nothing at runtime. No modal wiring, no form submission, no navigation target present
- [med] apps/saltgrass-modular/src/pages/services/pools.astro:16 — Hero image references /img/pools-hero.jpg (line 16) — this is a static asset path that must exist in the public/ directory at build time. No fallback or error handling if missing.
- [med] apps/saltgrass-modular/src/pages/services/traditional-builds.astro:16 — Hero image references /img/traditional-builds-hero.jpg (line 16) and inline <img> references /img/traditional-home-exterior.jpg (line 52) — both must exist as static assets in public/. No fallback or lazy loading specifi
- [high] apps/saltgrass-modular/src/pages/services/traditional-builds.astro:195 — CTA banner (line 195-198) says 'Schedule a consultation' but no link or modal trigger is present in this file. The consultation flow is not wired here — user has no navigation target from this CTA.
- [med] apps/web-marketing/src/pages/terms.astro:167 — Section 9 (Third-Party Integrations, lines 163-189) contains external links to Google, Meta, LinkedIn, and Microsoft developer terms pages as target='_blank' rel='noopener noreferrer'. These links are hardcoded and will 
- [med] apps/web-marketing/src/pages/terms.astro:85 — The /privacy link (line 85) and /pricing link (line 102) are relative hrefs — they depend on those routes existing in the same app. If the privacy or pricing pages are missing or at different paths, these links silently 
- [low] apps/web-marketing/src/pages/terms.astro:319 — IntersectionObserver active-TOC logic (lines 322-341) casts HTMLElement as HTMLAnchorElement inline — TypeScript 'as' cast without null guard on getAttribute. Low risk at runtime (elements are always anchors here) but fr
- [high] apps/web-marketing/src/pages/terms.astro:35 — The 'Last updated: May 4, 2026' date (line 35) is hardcoded. If the terms content is edited without updating this date, it becomes a misleading legal timestamp.
